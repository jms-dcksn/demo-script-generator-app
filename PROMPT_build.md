0. Read `specs/*` to learn the application specifications.
   Read @IMPLEMENTATION_PLAN.md to understand what needs to be done.
   Read source code for context.

1. Choose the most important item from @IMPLEMENTATION_PLAN.md and implement it.
   Before making changes, search the codebase to confirm the item isn't already done.
   Implement completely -- no placeholders or stubs.

2. Run the tests for the code you changed. If tests fail, fix them before moving on.
   If tests are missing for your changes, add them.

3. When done, update @IMPLEMENTATION_PLAN.md with your findings and mark completed
   items. Then `git add -A` and `git commit` with a descriptive message.

4. If you discover bugs unrelated to your current task, document them in
   @IMPLEMENTATION_PLAN.md for a future iteration.

5. Keep @IMPLEMENTATION_PLAN.md current -- future iterations depend on it to avoid
   duplicating work. Periodically clean out completed items.

6. If you learn something about how to build or run the project (commands, gotchas),
   update @AGENTS.md but keep it brief. Only operational info, not status updates.
